#include<stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{

    // double a;
    // scanf("%lf",&a);
    // int ans=ceil(a);
    // int ans=floor(a);
    // int ans=round(a);
    // double ans=sqrt(a);
    // printf("%0.3lf",ans);
    // int a,b;
    // scanf("%d %d",&a,&b);
    // int ans=pow(a,b);
    // printf("%d",ans);
    int a;
    scanf("%d",&a);
    int ans=abs(a);
    printf("%d ",ans);
    return 0;
}